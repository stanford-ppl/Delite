<DeliteExternalLib>
<compiler> /usr/local/cuda/bin/nvcc </compiler> <!-- path to nvcc -->
<make> /usr/bin/make </make>
<arch> 2.0 </arch> <!-- compute capability of gpu -->
<headers>
  <prefix> -I </prefix>
  <path> ${JAVA_HOME}/include </path> <!-- required for JNI -->
  <path> ${JAVA_HOME}/include/linux </path>
</headers>
</DeliteExternalLib>
