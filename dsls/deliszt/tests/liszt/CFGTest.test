import Liszt.Language._
import Liszt.MetaInteger._

@lisztcode
object CFGTest {
	
	def testfn() : Int = {
		val a = 5
		while(a < 5) {
			Print("no")
		} 
		if(a < 5) {
			if(a < 3) {
				return a
			}
		} else {
			return a
		}	
		return a
	}
	def main() {
		Print(testfn())
	}
}